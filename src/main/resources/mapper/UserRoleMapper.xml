<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tree.clouds.notification.mapper.UserRoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.tree.clouds.notification.model.entity.UserRole">
        <result column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
        <result column="create_user" property="createUser"/>
        <result column="create_time" property="createTime"/>
        <result column="update_user" property="updateUser"/>
        <result column="update_time" property="updateTime"/>
        <result column="del" property="del"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        user_id, role_id, create_user, create_time, update_user, update_time, del
    </sql>
    <select id="getRoleByUserId" resultType="com.tree.clouds.notification.model.entity.Role">
         select a.* from role a
            left join user_role c on a.role_id = c.role_id
            where c.user_id=#{userId}
    </select>
    <select id="getUserInfoByRole" resultType="com.tree.clouds.notification.model.entity.User">
        select b.* from role_user a
            left join user b on a.user_id = b.user_id
            left join role c on a.role_id = c.role_id
            where c.role_name=#{roleId}
    </select>

</mapper>
