<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tree.clouds.notification.mapper.RoleMenuMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.tree.clouds.notification.model.entity.RoleMenu">
        <result column="role_id" property="roleId"/>
        <result column="menu_id" property="menuId"/>
        <result column="create_user" property="createUser"/>
        <result column="create_time" property="createTime"/>
        <result column="update_user" property="updateUser"/>
        <result column="update_time" property="updateTime"/>
        <result column="del" property="del"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        role_id, menu_id, create_user, create_time, update_user, update_time, del
    </sql>
    <select id="getNavMenuIds" resultType="java.lang.String">
        SELECT DISTINCT c.menu_id from user a
            LEFT JOIN user_role  b on a.USER_ID=b.USER_ID
            LEFT JOIN role_menu c on b.ROLE_ID=c.role_id
            where a.user_id=#{userId}
    </select>

</mapper>
